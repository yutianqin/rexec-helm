{{- if and .Values.inClusterConfig.enabled .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "deployment-api.serviceAccountName" . }}
  labels:
    {{- include "deployment-api.labels" . | nindent 4 }}
{{- end }}
